server:
  port: 8080

spring:
  security:
    oauth2:
      authorization:
        server:
          issuer: http://localhost:8080

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
